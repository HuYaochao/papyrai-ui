---
description: papyrai-ui 开发进度追踪与规范
alwaysApply: true
---

# papyrai-ui 开发规范与进度

> 本规则自动应用于整个项目。每完成一项任务，都应同步更新本文档。

## 项目概览

- **定位**: 第一个面向 AI 应用场景的、框架无关、类纸风格 UI 组件库
- **名称**: papyrai-ui（papyrus + ai + ui）
- **协议**: MIT
- **技术栈**: Web Components + Lit + Rollup + VitePress
- **主题**: 亮色 + 暗色（CSS 变量驱动）
- **总目标规模**: 136（9 AI + 8 Logo 模板 + 50 SVG 图标 + 69 基础组件）

## 里程碑进度（对齐 02 开发步骤条）

### Step 0：项目初始化
- [x] 0.1 目录结构
- [x] 0.2 package.json
- [x] 0.3 Rollup 配置
- [x] 0.4 preview/index.html 骨架
- [x] 0.5 LICENSE / FONT_LICENSE / README

### Step 1：Core 样式系统
- [x] 1.1 `src/core/tokens.js`（亮/暗主题变量）
- [x] 1.2 `src/core/styles.css`（基础样式、字体）
- [x] 1.3 `src/core/base.js`（`PapyraiElement` 基类）

### Step 2：AI 专属组件（9/9）
- [x] 2.1 `ai-thinking`
- [x] 2.2 `ai-stream`
- [x] 2.3 `ai-hallucination`
- [x] 2.4 `ai-not-found`
- [x] 2.5 `ai-fake-error`
- [x] 2.6 `ai-confidence`
- [x] 2.7 `ai-diff`
- [x] 2.8 `ai-cost`
- [x] 2.9 `ai-model-badge`

### Step 3：SVG 体系
- [x] 3.1 `svg-logo`（8 种模板）
- [x] 3.2 `svg-icon`（当前演示集已实现并可渲染）
- [ ] 3.3 `src/icons/` 独立图标组件库（清单与导出仍需收敛到实际文件，避免无效 import）

### Step 4~9：基础组件（Elements / Inputs / Navigation / Overlays / Layout / Data）
- [ ] 尚未开始（待按 02 文档分批落地）

### Step 10：构建与发布
- [ ] 10.1 Rollup 产物校验（UMD/ESM/min）
- [ ] 10.2 按需引入产物
- [ ] 10.3 npm 发布流程
- [ ] 10.4 VitePress 文档站

## 当前已知问题与处理记录

- [x] 修复 `svg-icon` 渲染异常（`<svg ${string}>` 造成属性未正确生效，出现小黑点/图标缺失）
  - 处理：改为统一 `svgBase(...)` 包装器显式设置 `width/height/viewBox/stroke`。

## 命名与实现规范

- 标签前缀：AI 用 `ai-`、SVG 用 `svg-`、基础组件用 `p-`
- 文件名：kebab-case
- 基础约束：
  - 继承 `PapyraiElement`
  - `static properties` 完整声明
  - 支持亮/暗主题
  - 颜色优先使用 CSS 变量
  - 交互组件支持键盘操作与 slot（如适用）

## SVG 规范

- `viewBox="0 0 24 24"`
- `stroke-width="1.5"`
- `stroke="currentColor"`（允许通过属性覆写）
- `stroke-linecap="round"` + `stroke-linejoin="round"`

## 更新规则

1. 完成任务后更新对应步骤状态（`[ ]` → `[x]`）。
2. 如存在偏差（例如规划数与现状不一致），必须在“已知问题”记录。
3. 每次会话开始先读取本文件确认优先级与下一步。
